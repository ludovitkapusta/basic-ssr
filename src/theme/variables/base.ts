export interface IBase {
  baseFontSize: number
  baseFontColor: string
  baseWidth: number
  baseWidthWithSidebar: number
}

export const base: IBase = {
  baseFontColor: '#333',
  baseFontSize: 16,
  baseWidth: 1110,
  baseWidthWithSidebar: 815
}
